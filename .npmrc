# Security hardening: disable lifecycle scripts to prevent supply chain attacks
# This applies to npm ci and npm install in CI environments
# See: https://docs.npmjs.com/cli/using-npm/config#ignore-scripts
ignore-scripts=true
